{
  "name": "agentic-team-app",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "Simple Node app scaffold",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "npx nodemon src/index.js",
    "build": "echo 'Backend: no build step required' && cd frontend && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
